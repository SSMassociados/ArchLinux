Iniciar automaticamente com o sistema, configurando o teclado brasileiro como o teclado padrão 

sudo localectl set-x11-keymap br
sudo localectl set-x11-keymap br abnt2

e

setxkbmap -model abnt2 -layout br -variant abnt2

esse último pra vc aplicar com sua sessão atual

